const styles = theme => ({
    container: {
        display: 'flex',
        flexWrap: 'wrap',
    },    
    menu: {
        width: 0,
    },
    typography: {
        margin: theme.spacing.unit * 2,
    }
});

export default styles;